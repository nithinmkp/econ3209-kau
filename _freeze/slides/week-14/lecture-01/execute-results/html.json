{
  "hash": "fadeb9191f144d8a2624c8a3513f868c",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Log & Polynomial Models\"\nsubtitle: \"ECON 3209 · Week 14, Lecture 1 · Kerala Agricultural University\"\nauthor: \"Department of Agricultural Economics, KAU\"\ndate: \"Autumn 2026\"\nformat:\n  revealjs:\n    title-slide-attributes:\n      data-background-color: \"#1A3300\"\n---\n\n---\n\n## Learning Outcomes {background-color=\"#1A3300\"}\n\nBy the end of this lecture, you will be able to:\n\n1. distinguish linear, log, and polynomial specifications\n2. interpret coefficients in log-log, log-lin, and lin-log models\n3. recognize diminishing returns using quadratic terms\n4. estimate transformed models in Python\n5. compare alternative functional forms using economic reasoning and fit statistics\n\n---\n\n## Why Move Beyond Straight Lines?\n\nMany economic relationships are nonlinear:\n- fertilizer may show diminishing returns\n- income may rise proportionally with land only at low levels\n- percentage changes may matter more than absolute changes\n\nA model can still be linear in parameters while being nonlinear in variables.\n\n---\n\n## Log Models and Interpretation\n\n::: {.formula}\n$$\\ln Y_i = \\beta_0 + \\beta_1 \\ln X_i + \\varepsilon_i$$\n:::\n\nIn a log-log model, $\\beta_1$ is an **elasticity**.\n\n- 1% increase in $X$ is associated with a $\\beta_1$% change in $Y$.\n- Useful when scale differs greatly across observations.\n\n---\n\n## Other Common Transformations\n\n| Model | Equation | Interpretation of $\\beta_1$ |\n|---|---|---|\n| log-log | $\\ln Y = \\beta_0 + \\beta_1 \\ln X + u$ | elasticity |\n| log-lin | $\\ln Y = \\beta_0 + \\beta_1 X + u$ | one-unit rise in $X$ changes $Y$ by about $100\\beta_1$% |\n| lin-log | $Y = \\beta_0 + \\beta_1 \\ln X + u$ | 1% rise in $X$ changes $Y$ by about $0.01\\beta_1$ units |\n| quadratic | $Y = \\beta_0 + \\beta_1 X + \\beta_2 X^2 + u$ | slope depends on $X$ |\n\n---\n\n## Quadratic Model and Diminishing Returns\n\n::: {.formula}\n$$Y = \\beta_0 + \\beta_1 X + \\beta_2 X^2 + \\varepsilon$$\n:::\n\nIf $\\beta_2 < 0$, the curve bends downward and marginal returns fall as $X$ increases.\n\n::: {.formula}\n$$\\frac{\\partial Y}{\\partial X} = \\beta_1 + 2\\beta_2 X$$\n:::\n\n---\n\n## Python Demo: Estimate a Log-Log Model\n\n```{pyodide-python}\nimport numpy as np\nimport pandas as pd\nimport statsmodels.formula.api as smf\n\nnp.random.seed(61)\nn = 120\nland = np.random.uniform(0.5, 8, n)\nincome = np.exp(2.8 + 0.65*np.log(land) + np.random.normal(0, 0.15, n))\ndf = pd.DataFrame({'land': land, 'income': income})\n\nlog_model = smf.ols('np.log(income) ~ np.log(land)', data=df).fit()\nprint(log_model.params.round(4))\n```\n\n---\n\n## Python Demo: Compare Log-Log, Log-Lin, and Lin-Log\n\n```{pyodide-python}\nloglin = smf.ols('np.log(income) ~ land', data=df).fit()\nlinlog = smf.ols('income ~ np.log(land)', data=df).fit()\n\ncompare = pd.DataFrame({\n    'AIC': [log_model.aic, loglin.aic, linlog.aic],\n    'BIC': [log_model.bic, loglin.bic, linlog.bic]\n}, index=['log-log', 'log-lin', 'lin-log'])\nprint(compare.round(3))\n```\n\n---\n\n## Python Demo: Quadratic Fertilizer Response\n\n```{pyodide-python}\nfert = np.linspace(20, 220, 120)\nyield_ = 20 + 0.45*fert - 0.0012*(fert**2) + np.random.normal(0, 4, len(fert))\nquad = pd.DataFrame({'yield_': yield_, 'fert': fert})\nquad_model = smf.ols('yield_ ~ fert + I(fert**2)', data=quad).fit()\nprint(quad_model.params.round(5))\n```\n\n---\n\n## Python Demo: Plot the Quadratic Fit\n\n```{pyodide-python}\nimport matplotlib; matplotlib.use('module://matplotlib_pyodide.html5_canvas_backend')\nimport matplotlib.pyplot as plt\n\nxgrid = np.linspace(quad['fert'].min(), quad['fert'].max(), 200)\npred = quad_model.predict(pd.DataFrame({'fert': xgrid}))\n\nplt.figure(figsize=(6.5, 4))\nplt.scatter(quad['fert'], quad['yield_'], alpha=0.45)\nplt.plot(xgrid, pred, color='darkgreen', linewidth=2)\nplt.xlabel('Fertilizer')\nplt.ylabel('Yield')\nplt.title('Diminishing returns to fertilizer')\nplt.show()\n```\n\n---\n\n## Turning Point of a Quadratic\n\nThe slope becomes zero at\n\n::: {.formula}\n$$X^* = -\\frac{\\beta_1}{2\\beta_2}$$\n:::\n\nWhen $\\beta_2 < 0$, this is the peak of the fitted parabola.\n\n---\n\n## Python Demo: Compute the Turning Point\n\n```{pyodide-python}\nb1 = quad_model.params['fert']\nb2 = quad_model.params['I(fert ** 2)']\nturning_point = -b1 / (2*b2)\nprint(f'Estimated turning point: {turning_point:.2f}')\n```\n\n---\n\n## Practical Warnings\n\n- Logs require positive values.\n- Polynomial terms can overfit at the extremes.\n- Functional form should reflect both theory and data patterns.\n\n::: {.highlight-box}\nChoose nonlinear forms because they make economic sense, not just because they raise fit statistics.\n:::\n\n---\n\n## 🏋️ Exercise {background-color=\"#1A3300\"}\n\n::: {.gold-box}\n1. Estimate a log-log income model using land as the regressor.\n2. Interpret the elasticity estimate.\n3. Simulate a quadratic fertilizer response and estimate a polynomial model.\n4. Compute and interpret the turning point.\n:::\n\n```{pyodide-python}\nimport numpy as np\nimport pandas as pd\nimport statsmodels.formula.api as smf\n\nnp.random.seed(31)\nn = 100\nland = np.random.uniform(0.8, 6.5, n)\n\n# Build a positive income variable, fit a log-log model, and interpret beta_1\n```\n\n---\n\n## Summary {background-color=\"#1A3300\"}\n\n✅ Log models are useful when proportional or percentage effects are more natural than level effects.\n\n✅ In a log-log model, the slope is an elasticity.\n\n✅ Quadratic models allow marginal effects to vary with the level of the regressor.\n\n✅ A negative squared term captures diminishing returns.\n\n✅ Functional-form choice should combine theory, interpretation, and empirical fit.\n\n---\n\n## Next Lecture\n\n::: {.highlight-box}\n**Lecture 2 — Splines & Piecewise**\n\nWe will cover:\n- piecewise linear regression\n- spline intuition\n- knot selection\n- interpreting hinge terms\n:::\n\n",
    "supporting": [
      "lecture-01_files\\figure-revealjs"
    ],
    "filters": [],
    "includes": {}
  }
}